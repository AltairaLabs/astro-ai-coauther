---
/**
 * @ai-coauthor
 * @sourceContext
 * {
 *   "files": [
 *     "task-manager.ts",
 *     "types.ts"
 *   ],
 *   "folders": [],
 *   "confidence": "high",
 *   "lastUpdated": "2025-11-23T14:50:03.506Z"
 * }
 */
const { frontmatter } = Astro.props;
const title = 'Getting Started with Task Manager';
const description = 'Learn how to install and use the Task Manager library';
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{title}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
        }
        header {
            border-bottom: 2px solid #007acc;
            padding-bottom: 1rem;
            margin-bottom: 2rem;
        }
        h1 {
            color: #007acc;
            margin-bottom: 0.5rem;
        }
        h2 {
            color: #005a9e;
            margin-top: 2rem;
            margin-bottom: 1rem;
        }
        .description {
            color: #666;
            font-size: 1.1rem;
        }
        nav {
            background: #f6f8fa;
            padding: 1rem;
            border-radius: 6px;
            margin-bottom: 2rem;
        }
        nav ul {
            list-style: none;
        }
        nav li {
            display: inline-block;
            margin-right: 1rem;
        }
        nav a {
            color: #007acc;
            text-decoration: none;
        }
        nav a:hover {
            text-decoration: underline;
        }
        code {
            background: #f6f8fa;
            padding: 0.2rem 0.4rem;
            border-radius: 3px;
            font-family: 'Monaco', 'Courier New', monospace;
            font-size: 0.9em;
        }
        pre {
            background: #f6f8fa;
            padding: 1rem;
            border-radius: 6px;
            overflow-x: auto;
            margin: 1rem 0;
        }
        pre code {
            background: none;
            padding: 0;
        }
        .note {
            background: #fff8e1;
            border-left: 4px solid #ffc107;
            padding: 1rem;
            margin: 1rem 0;
        }
    </style>
</head>
<body>
    <header>
        <h1>{title}</h1>
        <p class="description">{description}</p>
    </header>

    <nav>
        <ul>
            <li><a href="/">Home</a></li>
            <li><a href="/getting-started">Getting Started</a></li>
            <li><a href="/api">API Reference</a></li>
            <li><a href="/examples">Examples</a></li>
        </ul>
    </nav>

    <main>
        <section>
            <h2>Installation</h2>
            <p>Install the Task Manager library using npm:</p>
            <pre><code>npm install task-manager</code></pre>
            <p>Or using yarn:</p>
            <pre><code>yarn add task-manager</code></pre>
        </section>

        <section>
            <h2>Quick Start</h2>
            <p>Here's a simple example to get you started:</p>
            <pre><code is:raw>import &lbrace; TaskManager, TaskPriority, TaskStatus &rbrace; from 'task-manager';

// Create a new task manager instance
const manager = new TaskManager();

// Create your first task
const task = manager.createTask(&lbrace;
  title: 'Learn Task Manager',
  description: 'Read the documentation and try examples',
  priority: TaskPriority.HIGH,
  tags: ['learning', 'documentation']
&rbrace;);

console.log(`Created task: $&lbrace;task.id&rbrace;`);

// Update the task status
manager.updateTask(task.id, &lbrace;
  status: TaskStatus.IN_PROGRESS
&rbrace;);

// Get all tasks
const allTasks = manager.getAllTasks();
console.log(`Total tasks: $&lbrace;allTasks.length&rbrace;`);</code></pre>
        </section>

        <section>
            <h2>Creating Tasks</h2>
            <p>Tasks can be created with various options:</p>
            <pre><code>const task = manager.createTask(&lbrace;
  title: 'Complete project',           // Required
  description: 'Finish all features',  // Optional
  priority: TaskPriority.URGENT,       // Optional (default: MEDIUM)
  tags: ['project', 'deadline'],       // Optional (default: [])
  dueDate: new Date('2024-12-31'),     // Optional
  assignee: 'John Doe'                 // Optional
&rbrace;);</code></pre>
        </section>

        <section>
            <h2>Filtering Tasks</h2>
            <p>Filter tasks based on multiple criteria:</p>
            <pre><code>// Get high priority tasks that are not done
const urgentTasks = manager.filterTasks(&lbrace;
  priority: [TaskPriority.HIGH, TaskPriority.URGENT],
  status: [TaskStatus.TODO, TaskStatus.IN_PROGRESS]
&rbrace;);

// Get tasks by assignee
const myTasks = manager.filterTasks(&lbrace;
  assignee: 'John Doe'
&rbrace;);

// Get tasks due soon
const tomorrow = new Date();
tomorrow.setDate(tomorrow.getDate() + 1);
const dueSoon = manager.filterTasks(&lbrace;
  dueBefore: tomorrow
&rbrace;);</code></pre>
        </section>

        <section>
            <h2>Task Statistics</h2>
            <p>Get insights about your tasks:</p>
            <pre><code>const stats = manager.getStatistics();

console.log(`Total tasks: $&lbrace;stats.total&rbrace;`);
console.log(`Completed: $&lbrace;stats.byStatus.done&rbrace;`);
console.log(`In progress: $&lbrace;stats.byStatus.in_progress&rbrace;`);
console.log(`Overdue: $&lbrace;stats.overdue&rbrace;`);
console.log(`Completion rate: $&lbrace;(stats.completionRate * 100).toFixed(1)&rbrace;%`);</code></pre>
        </section>

        <div class="note">
            <strong>Note:</strong> The Task Manager stores tasks in memory. For persistent storage, 
            you'll need to implement serialization/deserialization logic.
        </div>

        <section>
            <h2>Next Steps</h2>
            <ul>
                <li>Explore the <a href="/api">API Reference</a> for detailed documentation</li>
                <li>Check out <a href="/examples">Examples</a> for more use cases</li>
                <li>Learn about advanced filtering and statistics</li>
            </ul>
        </section>
    </main>
</body>
</html>
