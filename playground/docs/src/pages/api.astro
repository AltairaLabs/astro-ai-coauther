---
/**
 * @ai-coauthor
 * @sourceContext
 * {
 *   "files": [
 *     "task-manager.ts"
 *   ],
 *   "folders": [],
 *   "confidence": "high",
 *   "lastUpdated": "2025-11-23T14:08:27.920Z"
 * }
 */
const { frontmatter } = Astro.props;
const title = 'Task Manager API Documentation';
const description = 'Complete API reference for the Task Manager library';
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{title}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
        }
        header {
            border-bottom: 2px solid #007acc;
            padding-bottom: 1rem;
            margin-bottom: 2rem;
        }
        h1 {
            color: #007acc;
            margin-bottom: 0.5rem;
        }
        h2 {
            color: #005a9e;
            margin-top: 2rem;
            margin-bottom: 1rem;
            padding-top: 1rem;
            border-top: 1px solid #e1e4e8;
        }
        h3 {
            color: #333;
            margin-top: 1.5rem;
            margin-bottom: 0.5rem;
        }
        .description {
            color: #666;
            font-size: 1.1rem;
        }
        nav {
            background: #f6f8fa;
            padding: 1rem;
            border-radius: 6px;
            margin-bottom: 2rem;
        }
        nav ul {
            list-style: none;
        }
        nav li {
            display: inline-block;
            margin-right: 1rem;
        }
        nav a {
            color: #007acc;
            text-decoration: none;
        }
        nav a:hover {
            text-decoration: underline;
        }
        code {
            background: #f6f8fa;
            padding: 0.2rem 0.4rem;
            border-radius: 3px;
            font-family: 'Monaco', 'Courier New', monospace;
            font-size: 0.9em;
        }
        pre {
            background: #f6f8fa;
            padding: 1rem;
            border-radius: 6px;
            overflow-x: auto;
            margin: 1rem 0;
        }
        pre code {
            background: none;
            padding: 0;
        }
        .method {
            background: #fff;
            border: 1px solid #e1e4e8;
            border-radius: 6px;
            padding: 1rem;
            margin: 1rem 0;
        }
        .method-signature {
            background: #f6f8fa;
            padding: 0.5rem;
            border-radius: 3px;
            font-family: 'Monaco', 'Courier New', monospace;
            margin-bottom: 0.5rem;
        }
        .badge {
            display: inline-block;
            padding: 0.2rem 0.5rem;
            border-radius: 3px;
            font-size: 0.85rem;
            font-weight: 600;
            margin-right: 0.5rem;
        }
        .badge-class { background: #e7f3ff; color: #0366d6; }
        .badge-interface { background: #fff5e6; color: #d97706; }
        .badge-enum { background: #f0f4ff; color: #6366f1; }
    </style>
</head>
<body>
    <header>
        <h1>{title}</h1>
        <p class="description">{description}</p>
    </header>

    <nav>
        <ul>
            <li><a href="/">Home</a></li>
            <li><a href="/getting-started">Getting Started</a></li>
            <li><a href="/api">API Reference</a></li>
            <li><a href="/examples">Examples</a></li>
        </ul>
    </nav>

    <main>
        <section>
            <h2>TaskManager Class</h2>
            <p><span class="badge badge-class">Class</span></p>
            <p>The main class for managing tasks. Provides methods for creating, updating, filtering, and analyzing tasks.</p>

            <div class="method">
                <h3>createTask()</h3>
                <div class="method-signature">
                    createTask(options: CreateTaskOptions): Task
                </div>
                <p>Creates a new task with the specified options.</p>
                <h4>Parameters:</h4>
                <ul>
                    <li><code>options</code> - Task creation options including title, description, priority, tags, etc.</li>
                </ul>
                <h4>Returns:</h4>
                <p>The newly created <code>Task</code> object.</p>
                <h4>Example:</h4>
                <pre><code>const task = manager.createTask(&lbrace;
  title: 'Implement feature',
  priority: TaskPriority.HIGH,
  tags: ['backend', 'api']
&rbrace;);</code></pre>
            </div>

            <div class="method">
                <h3>updateTask()</h3>
                <div class="method-signature">
                    updateTask(id: string, options: UpdateTaskOptions): Task | undefined
                </div>
                <p>Updates an existing task with the specified options.</p>
                <h4>Parameters:</h4>
                <ul>
                    <li><code>id</code> - The ID of the task to update</li>
                    <li><code>options</code> - Update options (all fields are optional)</li>
                </ul>
                <h4>Returns:</h4>
                <p>The updated task, or <code>undefined</code> if the task was not found.</p>
            </div>

            <div class="method">
                <h3>filterTasks()</h3>
                <div class="method-signature">
                    filterTasks(filter: TaskFilter): Task[]
                </div>
                <p>Filters tasks based on the specified criteria.</p>
                <h4>Parameters:</h4>
                <ul>
                    <li><code>filter</code> - Filter criteria (status, priority, tags, assignee, due dates)</li>
                </ul>
                <h4>Returns:</h4>
                <p>An array of tasks matching the filter criteria.</p>
                <h4>Example:</h4>
                <pre><code>const tasks = manager.filterTasks(&lbrace;
  status: [TaskStatus.TODO, TaskStatus.IN_PROGRESS],
  priority: TaskPriority.HIGH,
  tags: ['urgent']
&rbrace;);</code></pre>
            </div>

            <div class="method">
                <h3>getStatistics()</h3>
                <div class="method-signature">
                    getStatistics(): TaskStatistics
                </div>
                <p>Calculates and returns statistics about all tasks.</p>
                <h4>Returns:</h4>
                <p>A <code>TaskStatistics</code> object containing metrics like completion rate, task counts by status/priority, and overdue count.</p>
            </div>
        </section>

        <section>
            <h2>Enums</h2>
            
            <div class="method">
                <h3>TaskStatus</h3>
                <p><span class="badge badge-enum">Enum</span></p>
                <p>Possible states for a task:</p>
                <ul>
                    <li><code>TODO</code> - Task is planned but not started</li>
                    <li><code>IN_PROGRESS</code> - Task is currently being worked on</li>
                    <li><code>DONE</code> - Task is completed</li>
                    <li><code>CANCELLED</code> - Task was cancelled</li>
                </ul>
            </div>

            <div class="method">
                <h3>TaskPriority</h3>
                <p><span class="badge badge-enum">Enum</span></p>
                <p>Priority levels for tasks:</p>
                <ul>
                    <li><code>LOW</code> - Low priority</li>
                    <li><code>MEDIUM</code> - Medium priority (default)</li>
                    <li><code>HIGH</code> - High priority</li>
                    <li><code>URGENT</code> - Urgent priority</li>
                </ul>
            </div>
        </section>

        <section>
            <h2>Interfaces</h2>
            
            <div class="method">
                <h3>Task</h3>
                <p><span class="badge badge-interface">Interface</span></p>
                <p>Represents a single task with all its properties.</p>
                <pre><code>interface Task &lbrace;
  id: string;
  title: string;
  description?: string;
  status: TaskStatus;
  priority: TaskPriority;
  tags: string[];
  createdAt: Date;
  updatedAt: Date;
  dueDate?: Date;
  assignee?: string;
&rbrace;</code></pre>
            </div>

            <div class="method">
                <h3>TaskFilter</h3>
                <p><span class="badge badge-interface">Interface</span></p>
                <p>Criteria for filtering tasks.</p>
                <pre><code>interface TaskFilter &lbrace;
  status?: TaskStatus | TaskStatus[];
  priority?: TaskPriority | TaskPriority[];
  tags?: string[];
  assignee?: string;
  dueBefore?: Date;
  dueAfter?: Date;
&rbrace;</code></pre>
            </div>
        </section>
    </main>
</body>
</html>
